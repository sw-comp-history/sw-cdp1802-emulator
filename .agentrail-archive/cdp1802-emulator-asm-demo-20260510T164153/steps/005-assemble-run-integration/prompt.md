Step 5 of saga `cdp1802-emulator-asm-demo`: assemble-run integration.\n\nGoal: add an integration test in `sw-cdp1802-emulator` that assembles the demo source with sibling `sw-cdp1802-asm`, loads the emitted bytes into emulator memory, runs to `IDL`, and asserts the contract final state.\n\nRead first:\n- `docs/demo-contract.md`\n- `tests/exec_units.rs` in this repo\n- `~/github/sw-comp-history/sw-cdp1802-asm/tests/asm.rs`\n- IBM 1130 assemble-load-run pattern in `~/github/sw-comp-history/sw-ibm1130-emulator/tests/run_demos.rs`\n\nWork for this step:\n1. Stay in this `sw-cdp1802-emulator` repo.\n2. Add `sw-cdp1802-asm` as a dev-dependency if needed.\n3. Add an integration test that uses the exact demo source from `docs/demo-contract.md`, calls `sw_cdp1802_asm::assemble`, asserts exact bytes, loads them at address 0, runs with a bounded step count, and asserts halted, RAM `0x2000..0x2002 == [0x42, 0x43, 0x44]`, and `R1 == 0x2002`.\n4. Do not add examples, demo runners, devices, timing, interrupts, or extra opcodes in this step.\n5. Run relevant cargo tests, commit emulator changes plus agentrail metadata, then complete the step.