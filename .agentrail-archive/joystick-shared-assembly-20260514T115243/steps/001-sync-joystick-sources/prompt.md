Update both joystick assembly sources so the CLI version and web UI version run the same valid CDP1802 program. The 1802 program must clear display RAM above code via OUT 1, pulse X/Y RC circuits with OUT 2/OUT 3, poll EF4 in counting loops, compute the video byte address within the 256-byte ELF-II memory page, and write the ball pixel itself with STR R1. Host Rust may emulate the IO board and render memory, but must not read joystick buckets to draw directly or use a separate video buffer. Keep the web UI and CLI behavior aligned, verify tests/demos, then commit the changes and complete this step.