Update sw-cdp1802-io so every demo has a concise description near the TV monitor. The Joystick description must end exactly with: WARNING: if you move the joystick up you will crash the ball into the running program corrupting it. Add a new first/default demo named Add, listed before Cassette in the alphabetized dropdown, with a tiny 1802 assembly program that adds two numbers and stops. The Add demo should have a Step button that advances one CPU instruction at a time, so users can step through the listing/register changes. Keep browser responsiveness intact, rebuild pages, and verify IO/emulator checks.