Step 2 of saga `io-board-demo`: runnable I/O board demo.\n\nGoal: add a runnable example that simulates the ELF-style CDP1802 I/O board described by the step 1 contract.\n\nRead first:\n- `docs/io-board.md`\n- the step 1 demo contract document\n- `examples/cdp1802_demo.rs`\n- `src/board.rs`\n- `src/video.rs`\n- `tests/front_panel.rs`\n\nWork for this step:\n1. Add an example, likely `examples/io_board_demo.rs`, that assembles the demo source with `sw-cdp1802-asm`, initializes `CpuState`, `Memory`, and `FrontPanel`, runs with `run_with_front_panel`, and renders `VideoView::elf_64x32()`.\n2. Print source, assembled bytes, run status, final registers, front-panel state, selected RAM bytes, and the text video frame.\n3. Keep the demo deterministic and small enough for review.\n4. Do not add new CPU opcodes, CDP1861 DMA/timing, interrupts, serial/cassette, or UI graphics.\n5. Run `cargo fmt --check`, `cargo test`, and `cargo run --example io_board_demo`.\n6. Commit code plus agentrail metadata, then complete the step.