Add the joystick RC oscilloscope visualization to sw-cdp1802-io. In the Joystick demo only, show a compact dual-track oscilloscope panel for the emulated RC networks: top trace for X and bottom trace for Y. The trace should trigger when the 1802 program writes to the corresponding output pulse port through the shared emulator BoardIo path, rise at trigger time, and drop after the emulated RC delay determined by the current joystick potentiometer value. Keep the browser yield model intact: do not run tight CPU loops on the UI thread; continue executing only bounded CPU steps per timer/requestAnimationFrame callback. Do not fake CPU memory/register effects; Rust may visualize board signals and RC timing, while the 1802 assembler program remains responsible for polling EF4 and moving the ball. Add focused tests for waveform trigger/drop timing and keep existing demos passing.