Expand sw-cdp1802-isa from the current demo subset to the full RCA CDP1802 opcode map. Implement instruction enum variants, decode, encode, and disassembly for every CDP1802 opcode family documented in sw-cdp1802-emulator/docs/instruction-coverage.md. Add exhaustive decode coverage over 0x00..=0xff and encode/decode round-trip tests for every instruction family. Do not add emulator-only or demo-only instruction shortcuts; this step owns the shared ISA crate surface needed by assembler and emulator follow-up steps.