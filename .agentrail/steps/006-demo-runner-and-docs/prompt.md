Step 6 of saga `cdp1802-emulator-asm-demo`: demo runner and docs.\n\nGoal: add a small runnable example or test fixture in `sw-cdp1802-emulator` that prints the CDP1802 demo source, assembled bytes, execution step count, final registers, and relevant RAM range, mirroring the IBM 1130 demo discipline without expanding emulator semantics.\n\nRead first:\n- `docs/demo-contract.md`\n- `tests/run_demo.rs`\n- IBM 1130 demo runner pattern in `~/github/sw-comp-history/sw-ibm1130-emulator/examples/_common.rs`\n\nWork for this step:\n1. Stay in this `sw-cdp1802-emulator` repo.\n2. Add a minimal example or fixture that uses the existing assembler/emulator path and prints source, bytes, step count, final D/P/X/R1, and RAM `0x2000..0x2002`.\n3. Keep output deterministic and ASCII-only.\n4. Update docs only if needed to tell users how to run the demo.\n5. Do not add devices, timing, interrupts, extra opcodes, or frontend/codegen work in this step.\n6. Run relevant cargo tests and, if adding an example, `cargo run --example <name>`; commit changes plus agentrail metadata, then complete the step.