Implement the 4K cassette-loader logo demo using the shared full assembler and emulator path. Add memory-map configuration for base 256-byte ELF-II mode and expanded 4K mode, with the 4K demo loading code at 0x0000 and using a video page at 0x0100 so code bytes are not displayed as pixels. Rust may emulate cassette hardware and byte-available timing through BoardIo, but the 1802 assembler program must perform the actual reads and stores into video memory; do not fake the load by copying bytes directly into video memory. The demo should gradually show a 256-byte logo loading from a simulated cassette stream, then halt or loop. Keep browser responsiveness intact by executing only bounded CPU steps per UI callback. Add tests/build checks that prove cassette bytes are consumed by the CPU program and written to the video page through emulator execution.