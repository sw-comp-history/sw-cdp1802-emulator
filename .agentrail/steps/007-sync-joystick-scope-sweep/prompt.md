Correct the joystick RC oscilloscope in sw-cdp1802-io so the sweep is synchronized. Pulses must not drift right-to-left across the scope as time advances. For each observed RC pulse event, render X and Y traces with their leading edges left-aligned at the same sweep origin; the joystick position should vary pulse width/drop time, not horizontal start position. It is acceptable to show more than one captured sweep, but each pulse pair starts at the same left origin. Continue using real OUT 2/OUT 3 events from the shared emulator BoardIo path for trigger data, do not fake CPU memory/register behavior, and keep browser execution yielding one bounded CPU step per callback. Add or update tests proving the rendered/sample model has left-aligned leading edges and width varies with RC delay.