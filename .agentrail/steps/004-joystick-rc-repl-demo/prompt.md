Step 4 of saga `io-board-demo`: joystick RC timing REPL demo.\n\nGoal: implement a terminal demo that simulates the user\'s ELF-II-style joystick circuit: Rust emulates the joystick potentiometers and resistor-capacitor timing behavior, while 1802 assembly pulses output lines and polls input lines to measure X/Y delay and move a ball in the RAM-backed video buffer.\n\nRead first:\n- `docs/io-board.md`\n- `docs/io-board-demo-saga.md`\n- `docs/io-board-demo-contract.md`\n- `examples/io_board_demo.rs`\n- `src/board.rs`\n- `src/video.rs`\n- existing tests under `tests/`\n\nWork for this step:\n1. Design and implement a small Rust board/circuit model for a two-axis analog joystick. Represent X and Y potentiometer values deterministically, and expose an RC-style measurement behavior where 1802 code sends a pulse on an output path for each axis and then polls an input path until the simulated signal becomes visible after a delay derived from that axis value.\n2. Add only the minimal emulator/board hooks needed for this demo. Keep the 2D ball movement logic in CDP1802 assembly, not Rust. Rust may provide the emulated joystick circuit, REPL input, emulator stepping, and screen rendering.\n3. Add a runnable REPL example, likely `examples/joystick_rc_demo.rs`, that prompts for joystick X and Y values, runs the 1802 measurement/movement loop for one frame, and renders the 64 x 32 screen grid using spaces for clear pixels and a solid block for set pixels. If a solid block introduces encoding problems in source, use a clearly documented ASCII fallback and explain why.\n4. The display should show a single ball pixel or compact marker whose position changes according to the measured joystick values. Keep output deterministic for given X/Y inputs.\n5. Add focused tests for the RC timing model and at least one assembled joystick demo frame, asserting final ball position and rendered screen content.\n6. Document the port/pin mapping used by the joystick circuit. Do not implement a Web UI, CDP1861 timing/DMA, interrupts, serial/cassette, or unrelated CPU opcodes.\n7. Run markdown validation, `cargo fmt --check`, `cargo test`, and the runnable example in a non-interactive/smoke-test mode if the REPL supports one.\n8. Commit code/docs plus agentrail metadata, complete the step with `--done`, and push.