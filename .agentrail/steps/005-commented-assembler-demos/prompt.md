Follow-up step for saga `io-board-demo`: commented assembler-backed demos.\n\nGoal: redo the runnable demos so they clearly use the assembler and include useful comments in the assembly source shown to users.\n\nRead first:\n- `examples/cdp1802_demo.rs`\n- `examples/io_board_demo.rs`\n- `examples/joystick_rc_demo.rs`\n- existing tests under `tests/` that consume demo source\n- `README.md`\n\nWork for this step:\n1. Ensure runnable demos assemble source through `sw-cdp1802-asm`; do not replace them with hard-coded byte demos.\n2. Add concise comments to the assembly source strings in each runnable demo, especially the I/O board and joystick demos, so users can understand the pulses, polling, RAM writes, and video effects.\n3. Keep tests aligned with the source strings and exact byte expectations.\n4. Update README only if wording needs to clarify that demos assemble source at runtime.\n5. Run markdown validation if README/docs change, plus `cargo fmt --check`, `cargo test`, and the runnable demos in non-interactive form where applicable.\n6. Commit changes plus agentrail metadata, complete the step with `--done`, and push.