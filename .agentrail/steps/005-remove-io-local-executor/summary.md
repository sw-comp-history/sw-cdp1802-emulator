Refactored sw-cdp1802-io browser demos to use the shared sw-cdp1802-emulator stepper instead of local opcode execution. Added a generic emulator board stepping API, implemented the web board through BoardIo, preserved joystick/logo/pattern behavior, rebuilt pages assets, and added a regression test that each browser frame step executes only one CPU instruction so the browser yields back to the UI thread.