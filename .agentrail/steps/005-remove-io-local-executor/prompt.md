Refactor sw-cdp1802-io so browser demos use the shared sw-cdp1802-emulator single-step path instead of maintaining duplicate demo-local instruction execution. Remove or shrink local opcode semantics in sw-cdp1802-io/src/demo.rs, wire the demo loop through the shared emulator state/memory APIs, and preserve browser responsiveness by executing only a bounded number of CPU instructions per timer/request-animation-frame callback. Keep existing Joystick and Logo behavior passing, with no fake memory writes outside 1802 program/device behavior. Add or update tests/build checks for the web crate as appropriate. Do not implement new demos or the oscilloscope visualization in this step.