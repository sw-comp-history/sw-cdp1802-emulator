Update sw-cdp1802-io so the demo dropdown lists demos alphabetically and the Cassette demo includes a compact audio waveform scope showing the simulated tape signal as it plays. The cassette waveform should be derived from the cassette byte stream/read progress in Rust board telemetry, not from fake CPU memory writes; the 1802 program must still consume bytes through INP 4. Keep browser responsiveness intact by continuing one bounded CPU step per callback. Add/update tests for alphabetical demo ordering data where practical and cassette waveform progression, rebuild pages, and verify the IO and emulator crates.