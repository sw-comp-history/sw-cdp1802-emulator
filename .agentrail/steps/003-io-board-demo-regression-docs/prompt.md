Step 3 of saga `io-board-demo`: regression test and docs.\n\nGoal: make the I/O board demo durable and discoverable.\n\nRead first:\n- the step 1 demo contract document\n- `examples/io_board_demo.rs`\n- existing integration tests under `tests/`\n- `README.md`\n\nWork for this step:\n1. Add or tighten a regression test that exercises the same assembled program and asserts final CPU, front-panel, RAM, and video-rendering state.\n2. Update README and/or docs so a user can run the I/O board demo and understand the output.\n3. Keep the docs concise and ASCII-only.\n4. Run markdown validation, `cargo fmt --check`, `cargo test`, and the runnable example.\n5. Commit changes plus agentrail metadata, complete the step with `--done`, and push.