Update sw-cdp1802-io scope behavior: make joystick RC pulses about twice as wide visually, and correct the cassette scope so it starts black and draws green pulse waveforms for cassette tones instead of a solid green block with black bars. Verify whether the current model is square-wave based and document that briefly in the final response. Rebuild pages, run relevant checks, commit and push the web repo, then complete this saga.