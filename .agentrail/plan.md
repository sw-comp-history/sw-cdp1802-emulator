Update sw-cdp1802-io so the README includes a Playwright-captured Logo demo screenshot at ./images/logo-screenshot.png with a ?ts=<ms-since-epoch> cache-busting link generated by ep2ms. Also change the Cassette demo so loading the demo shows a flat tape scope initially, a Run button starts the cassette loader, the scope oscillates while loading, and the scope is flat again after halt. Rebuild pages, run relevant checks, commit and push the web repo, then record this saga.